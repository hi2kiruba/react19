import React from "react";

const Navbar = () => {
  return (
    /*
     * <nav> element for the top navigation bar.
     */
    <nav className="navbar">
      <h1>React Hooks!</h1>
    </nav>
  );
};

export default Navbar;
